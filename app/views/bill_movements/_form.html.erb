<%= form_for(@bill_movement) do |f| %>
  <% if @bill_movement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bill_movement.errors.count, "error") %> prohibited this bill_movement from being saved:</h2>

      <ul>
      <% @bill_movement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :total %><br>
    <%= f.text_field :total %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
